package model.interfaces;

public interface SoundBehaviour {
    /**
     * The animal makes a sound
     * @return The sound as String (empty String in case animal cannot make a sound)
     */
    String makeSound();
}
